<!-- START OF FILE views/error/500.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= typeof title !== 'undefined' ? title : 'Server Error' %></title>
</head>
<body>
    <div class="container">
        <h1>500 - Server Error</h1>
        <p><%= typeof message !== 'undefined' ? message : 'Sorry, something went wrong on our end. Please try again later.' %></p>

        <!-- Optional: Show error details only in development -->
        <% if (process.env.NODE_ENV !== 'production' && typeof error !== 'undefined') { %>
            <hr>
            <h2>Error Details (Development Mode):</h2>
            <pre><%= error.stack %></pre>
        <% } %>

         <p><a href="/">Go to Homepage</a></p>
         <% if (currentUser) { %>
             <p><a href="/dashboard">Go to Dashboard</a></p>
         <% } %>
    </div>
</body>
</html>
<!-- END OF FILE views/error/500.ejs -->